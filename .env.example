# --- Video generation (fal.ai) ---
# Get a key at https://fal.ai/dashboard/keys
# Without this, the app runs in DEMO mode (returns a sample clip).
FAL_KEY=

# Optional: override the image-to-video model.
# Cheaper models = higher margin. Avoid expensive ones for free-tier clips.
# FAL_MODEL=fal-ai/kling-video/v1.6/standard/image-to-video
# FAL_MODEL_FREE=fal-ai/kling-video/v1.6/standard/image-to-video

# --- Session / credits (signed cookie) ---
# Generate with: openssl rand -base64 32
SESSION_SECRET=

# --- Stripe billing ---
# STRIPE_SECRET_KEY=sk_test_...
# STRIPE_PRICE_CREATOR=price_...
# STRIPE_PRICE_SHOP=price_...
# STRIPE_WEBHOOK_SECRET=whsec_...
# Webhook URL: https://YOUR_DOMAIN/api/webhooks/stripe
# Events: checkout.session.completed, invoice.paid,
#         customer.subscription.deleted, customer.subscription.updated
# NEXT_PUBLIC_SITE_URL=https://pikbo.ai

# Durable entitlements file (default: ./data/entitlements.json)
# ENTITLEMENTS_PATH=/var/data/entitlements.json

# Force-enable dev plan upgrades even in production (not recommended)
# ALLOW_DEV_UPGRADE=1
