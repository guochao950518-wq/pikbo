# GPT — you are one of three agents on Pikbo

1. `git fetch && git pull` from https://github.com/guochao950518-wq/pikbo
2. Read `COLLAB.md` + claim work in `docs/STATUS.md`
3. Prefer homepage / demo media / conversion UX unless you claim another row
4. Commit with `[gpt]` prefix; branch `agent/gpt/<topic>`
5. Log reusable wins in `docs/HANDOFF.md`

Do not rewrite credits/session APIs without reading `docs/HANDOFF.md` first.
