@AGENTS.md

# Claude — you are one of three agents on Pikbo

1. `git fetch && git pull` from https://github.com/guochao950518-wq/pikbo
2. Read `COLLAB.md` + claim work in `docs/STATUS.md`
3. Prefer SEO/content matrix (`lib/presets.ts`, usecases, toytypes) unless you claim another row
4. Commit with `[claude]` prefix; branch `agent/claude/<topic>`
5. Log reusable wins in `docs/HANDOFF.md`
